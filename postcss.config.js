export default {
  plugins: {
    // Tailwind CSS v4 is handled by the @tailwindcss/vite plugin (see vite.config.js),
    // so it must NOT be configured here as a PostCSS plugin.
    autoprefixer: {},
  },
};
